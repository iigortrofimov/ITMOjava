package other.tests.etc.dao_example;

/*
public class User {
    int id;
    String login;

    public int getId() {
        return id;
    }

    public void setId(int id) {
        this.id = id;
    }

    public String getLogin() {
        return login;
    }

    public void setLogin(String login) {
        this.login = login;
    }
}

class Article { // статьи
    int id;
    String title;
    int idUser;

    public int getId() {
        return id;
    }

    public void setId(int id) {
        this.id = id;
    }

    public String getTitle() {
        return title;
    }

    public void setTitle(String title) {
        this.title = title;
    }

    public int getIdUser() {
        return idUser;
    }

    public void setIdUser(int idUser) {
        this.idUser = idUser;
    }
}

// interface Dao{
//    1. add();
//    5.delete();
//    4.update();
//    2. getAll();
//    3. 2getById();
// }
//
// class UserDao implements Dao{ // for adding user
//    //реализация методов интерфейса

// create TABLE String sql= "CREATE TABLE IF NOT EXISTS USER (" +
//"id INTEGER PRIMARY"
//     getById(){
//         String sql = "SELECT * FROM USER WHERE id = 1";
//         String insert = "INSERT INTO USER (id, login) VALUES (?, ?);"
//         String getAll = " SELECT * from USER;"
//         String getById = " SELECT * FROM USER WHERE id=?;"
//         String update = " UPDATE USER SET id=?, login=? where id=?"

//     }
// }
// class ArticleDao implements Dao{ // for adding articles;
//     //реализация методов интерфейса
// }

1. регистрировать драйвер
2. Получить объект соединения
3.Создать объект statmaent / prepareStatment
Statment
4. У объекта статмент вызвать методы executeUpdate или executeQuery () в случаях полученя*/
