package lesson11;

import com.sun.org.apache.xpath.internal.operations.String;

import java.util.ArrayList;
import java.lang.*;

public class CollectionsExample {
    public static void main(String[] args) {
        /* Java Collectiom FrameWork
         java,util,collection
        boolean add(E e)
        boolean remove(Object o)
         int size();
       boolean isEmpty;
         boolean contains(Object o);

         Object[] toArray[];
         <T> T[] to Arrat(T[] a);
        String[] arr = collectiomName.toArray(new String[0]);

         iterator()  что бы ходить по элементам коллекции
         java.util.List предоставляет методы для работы с коллекцией, которая является списком
         в List элементы могут повторять; упорядочный набор элементов*/
        /*void add(int ind, E obj);
        boolean addAll(int ind, Collection <? extends E> col)
        E get(int ind);
        int indexOf(Object o);
        int lastindexOf(Object o);
        void add(int ind, E obj);
        E remove (int ind);
        E set(int index, E obj);*/
        //     Lise<E> sublist(int start, int end);
        //----------------------------------------------------------------

        ArrayList<String> list = new ArrayList<>();
//        list.ensureCapacity(20); // минимальный размер списка
//        list.trimToSize();// обрезет свободные ячейки
//        list.add("Elem2");
////        list.add("Elem3");
//        list.add("Elem4");
//        System.out.println(list.toString());
//        java.lang.String


    }
}
